[pytest]
# `.` puts otmlib on the path, `garminsvc` puts the garminsvc package and its
# top-level `tests` package there — the same layout Docker gets under /app.
pythonpath = . garminsvc
testpaths = otmlib/tests tilesvc/tests garminsvc/tests
python_files = test*.py
python_functions = test*
