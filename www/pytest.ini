[pytest]
# `.` is the only import root: otmlib, tilesvc and garminsvc are all packages
# under www/ — the same layout Docker gets under /app.
pythonpath = .
testpaths = otmlib/tests tilesvc/tests garminsvc/tests
python_files = test*.py
python_functions = test*
